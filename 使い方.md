# 筋トレ・栄養管理アプリ 使い方

## スマホで使う（公開手順）

既存の筋トレ記録アプリと同じく **GitHub Pages** で公開できます。

1. GitHub（to104）で新しいリポジトリを作成（例: `fitfuel`）
2. このフォルダの中身をすべてアップロード
3. Settings → Pages → Branch を `main` にして保存
4. 数分後に `https://to104.github.io/fitfuel/` で開ける
5. **iPhone**: Safariで開く → 共有ボタン → 「ホーム画面に追加」
   **Android**: Chromeで開く → メニュー → 「アプリをインストール」

> 💡 **PWA**: ホーム画面に追加するとネイティブアプリのように全画面・オフラインで動きます。

## 毎日の流れ

| いつ | すること |
|------|---------|
| 朝 | ホームの「今日の体重」→ 体重（＋体脂肪率・筋肉量）を入力 |
| 食事のたび | ホームの「朝食/昼食/夕食/間食」→ 検索 or 履歴からワンタップ登録 |
| 水を飲んだら | ホームの「＋250ml」などをタップ |
| トレの日 | トレタブ → 種目を選んでセット入力（前回記録が自動で入る） |
| 夜 | ホームのアドバイスで不足栄養素を確認 |

- **3タップ記録**: ホームの食事ボタン → 履歴タブ → 品目タップ、で完了。
- 記録した品目をタップすると分量修正・削除ができます。

## データについて

- データはすべて**この端末の中**（IndexedDB＝ブラウザ内蔵データベース）に保存されます。
- **週1回のバックアップ推奨**: 設定 → 「バックアップをエクスポート」でJSONファイルを保存し、iCloudやGoogleドライブに保管してください。
- 機種変更時は新しい端末で「バックアップをインポート」すれば全データが復元されます。
- ブラウザの履歴・サイトデータを削除すると記録も消えるので注意。

## 栄養値について

- 内蔵の食品データ（約150品目）は**日本食品標準成分表（八訂）ベースの近似値**です。
- パッケージに栄養成分表示がある食品は、「手入力」→「Myフードにも保存」で正確な値を登録すると、次回から検索で出てきます。

## 更新時の注意（開発メモ）

- `js/app.js` の `APP_VER` と `sw.js` の `CACHE` 名を**両方**上げること（筋トレ記録アプリと同じルール）。
- アイコンを作り直す場合: `node tools/gen-icons.mjs`

## 今後の予定（設計書より）

- Phase 2: AI写真解析（Claude APIキー取得後）・バーコード読み取り
- Phase 3: 通知・クラウド同期
- 詳細は `docs/設計書.md` を参照
